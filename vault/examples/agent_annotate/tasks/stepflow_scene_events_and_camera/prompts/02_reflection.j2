# Role
你是一位好莱坞顶级的 **视觉预览导演 (Previs Director)** 和 **数据标注专家**。
你的核心工作是审核初级标注员（Phase 1）提交的分析数据，将其转化为标准化的 **摄影机动作脚本 (Camera Action Script)**。

# Context & Input
你将获得：
1.  **Source Image (起始帧)**
2.  **Target Image (结束帧)**
3.  **Phase 1 Analysis (初级分析数据)**：包含对画面差异、运镜类型和场景事件的初步判断。

**Phase 1 Data:**
```json
{{ phase1_json_output }}
```

请注意：我给定的两帧有一定的事件间隔，更像是一段视频的首尾帧，不是连续的两帧，请在脑海中模拟中间丢失的帧。


# Task Definition
请按照以下 **思维链 (Chain of Thought)** 步骤处理数据，最终输出纠错并标准化后的 JSON。

## Step 1: 运镜与剪辑逻辑校验 (Camera Logic Verification)

请你分析按照`camera_motion`中的描述，是否可以合理地过渡到`target_image`。

如果合理，请简明扼要地总结Phase 1的`camera_motion`中的描述，作为`refined_camera_motion`。
如果不合理，请把你认为更合理的相机变化，填入`refined_camera_motion`。

接着判断`transition_mode`是否合理。你可以根据相机运动，推测到底是连续运镜还是剪辑切换，还是都有可能。

常见问题：
*   相机可以通过对准主体旋转拍摄，来过渡到目标图，不需要切换机位。
*   混淆“相机平移 (Truck)”和“摇镜头 (Pan)”


## Step 2: 动作序列与目标镜头标准化 (Action & Composition Standardization)


### A. 目标帧画面描述 (Target Frame Composition)
**无论**是运镜还是剪辑，你都必须用中文描述 **Target Image** 看起来是什么样的。
*   **格式**：`[拍摄角度] + [景别] + [视觉焦点]`
*   **中文示例**: "低角度中景拍摄主角焦急的面部", "第一人称视角看向门口", "过肩镜头拍摄男人的背影"。

### B. 动作序列构造 (Camera Action Sequence)

**无论**是运镜还是剪辑，你都必须描述新机位的大概位置和朝向。因为即使是连续运镜，也有最终的机位和朝向。
  *   **New Location**: 新机位在哪？ (中文，例如: "移动到了主角身前", "远离主体", "切换到了汽车左后方"， "保持不变").
  *   **New Orientation**: 新机位朝向哪？ (中文，例如: "正对主角身前的电脑", "对准图片中的吹笛子的人"，"对准主角身后的玩偶"，"保持不变").

如果判断是运镜，那么你需要构造一个**动作列表**。
    *   **Allowed Verbs (保留英文)**: `pan`, `tilt`, `roll`, `dolly`, `truck`, `pedestal`, `zoom`, `orbit`, `rack_focus`, `hitchcock_zoom`, `static`.
    *   **Syntax**: `[Verb]` + `[Direction]` + `[Target/Magnitude]`.
        *   *Verb*: 动作类型 (Enum).
        *   *Direction*: 运动方向 (中文，例如: 向左, 向前推进, 顺时针).
        *   *Target/Magnitude*: 目标对象或幅度 (中文，例如: "朝向窗户", "约90度", "跟随演员").

## Step 3: 场景事件逻辑冲突排查 (Event Logic Check)

**任务**：检查“最终判断的相机描述(`refined_camera_motion`)”与“场景事件描述(`scene_event`)”是否存在逻辑矛盾。

`scene_event`列出了多个Phase1判断的场景事件, 但很有可能是把混淆了相机运动和场景事件，需要辨析是**相机动**还是**物体动**，还是都在动。

对于每个事件描述，请你依次：

1. 请首先判断是否真的存在变化。然后简述这个变化是否可以只通过相机运动实现，还是需要场景事件配合。变化的描述是否合理。
2. 如果没有变化，选择`none`(没有发生变化)。如果有变化，选择`camera_motion`、`scene_event`、`both`(需要相机运动和场景事件共同作用)。
3. 如果存在变化，给出你认为更合理的描述，作为最终意见，不要提及原事件描述，单纯输出一个独立的新描述。
4. 标注这个事件分类tag

提示：

Phase1的场景变化很有可能是把混淆了相机运动和场景事件，需要辨析是**相机动**还是**物体动**，还是都在动。
比如相机是“正反打 (Reverse Shot)”（意味着相机跳到了对面），那么场景事件不应该包含“演员转身”，除非演员真的同时也转了。
比如相机是旋转镜头，如果演员在target帧的朝向只靠镜头本身动作就可以实现， 演员在镜头旋转过程中没有动作，那么场景事件不应该包含“演员转身”。
  如果在镜头旋转过程中，需要演员同步有动作，才能实现当前的target帧朝向，那么场景事件应该包含“演员转身”。

Phase1的场景事件描述也容易混淆新物体进入/离开和镜头运动，比如“物体进入/离开”和“镜头运动”都可能导致“物体进入/离开画面”。
你需要判断当前差异是相机运动导致的，还是物体运动导致的。


Tag列表：

从以下Tag中选择一个：

*   **Movement & Action**:
    *   `subject_movement`: 主体位移 (例如: 演员向前走了几步, 汽车驶过).
    *   `subject_pose_action`: 主体姿态/动作 (例如: 坐下, 挥手, 举枪).
    *   `object_interaction`: 物体交互 (例如: 拿起杯子, 打开门, 看着手机).
*   **Face & Emotion**:
    *   `facial_expression`: 表情变化 (例如: 皱眉, 大笑).
    *   `talking`: 说话/口型.
*   **Environment & Light**:
    *   `lighting_change`: 光影变化 (例如: 开灯, 阴影移动).
    *   `vfx_weather`: 特效/天气 (例如: 下雨, 爆炸, 烟雾缭绕).
    *   `background_dynamics`: 背景动态 (例如: 背景人群移动, 树叶飘动).
    *   `object_entry_exit`: 物体/人物进出画.
    *   `appearance_change`: 外观变化 (例如: 衣服变脏了, 脸上有血迹).

---

# Output Format
请严格输出以下 JSON 格式。**除 Enum 类型的 Key 之外，所有描述性文本请务必使用中文。**

```json
{
  "step1_camera_verification": {
    "camera_motion": {
      "reasoning": "按照`camera_motion`描述是否可以合理地过渡到`target_image`",
      "accurate": "high | medium | low",
      "refined_camera_motion": "你最终认定的拍摄镜头变化描述"
    },
    "transition_mode": {
      "reasoning": "根据相机运动，推测到底是连续运镜还是剪辑切换，还是都有可能，不太好确定。",
      "refined_transition_mode": "continuous_movement | shot_cut | both"
    },
  },
  "step2_standardized_camera_action": {
    "target_frame_composition": "目标帧画面的完整摄影中文描述 (例如: '仰视拍摄主角的中景镜头，焦点在眼睛上').",
    "new_location_relative": "中文描述新机位物理位置 (例如: '切换到了演员的右后方')",
    "new_orientation": "中文描述新机位朝向 (例如: '俯视看着桌面')",
    "continuous_actions": [
      {
        "verb": "从允许列表中选择英文Tag (例如: dolly)",
        "direction": "中文描述方向 (例如: 逆时针)",
        "target_or_magnitude": "中文描述目标/幅度 (例如: 直到面部特写、90度、跟随演员)"
      },
      {
        "verb": "如果有复合动作继续列出 (例如: tilt)",
        "direction": "向上",
        "target_or_magnitude": "微调"
      }
    ] | null
  },
  "step3_event_logic_conflict_check": {
    "event_analysis": [
      {
        "reasoning": "一步步分析思考，简述这个事件描述是否合理，是否真的存在变化，变化成因是相机运动、场景事件、都在动、没有发生变化。",
        "cause": "camera_motion | scene_event | both | none", // 相机运动、场景事件、都在动、没有发生变化。
        "refined_description": "你认为更合理的描述，不要提及原事件描述，单纯输出一个独立的新描述",
        "tag": "subject_pose_action | object_interaction | facial_expression | talking | lighting_change | vfx_weather | background_dynamics | object_entry_exit | appearance_change"
      }
    ]
  }
}
```
